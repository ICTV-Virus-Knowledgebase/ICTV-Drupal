id: upload-sequences
plugin_id: upload-sequences
granularity: method
configuration:
  get:
    supported_formats: [json]
    supported_auth: [cookie, basic_auth, jwt_auth, oauth2]
  post:
    supported_formats: [json]
    supported_auth: [cookie, basic_auth, jwt_auth, oauth2]