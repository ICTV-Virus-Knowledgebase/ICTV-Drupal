name: ICTV JWT Generator
type: module
description: 'A custom module that displays a form to generate a JSON Web Token (JWT)'
package: ICTV
core_version_requirement: ^9 || ^10 || ^11
dependencies:
  - drupal:block
  - jwt:jwt_auth_issuer
